<Window x:Class="ExchangeRate.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExchangeRate.Desktop"
        mc:Ignorable="d"
        Title="Exchange Rate Monitor"
        Height="600"
        Width="900"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0"
                Orientation="Horizontal"
                Margin="0,0,0,10">
            <TextBlock Text="📊 Exchange Rate Monitor"
                    FontSize="24"
                    FontWeight="Bold"
                       VerticalAlignment="Center"
                    Margin="0,0,20,0"/>
            <Border x:Name="WorkerStatusBorder"
                    CornerRadius="5"
                    Padding="10,5"
                    VerticalAlignment="Center">
                <TextBlock x:Name="WorkerStatusLabel"
                        FontSize="14"
                        FontWeight="Bold"/>
            </Border>
        </StackPanel>

        <!-- Controls -->
        <StackPanel Grid.Row="1"
                Orientation="Horizontal"
                Margin="0,0,0,10">
            <Button x:Name="RefreshButton"
                    Content="🔄 Refresh Now"
                    Width="120"
                    Height="30"
                    Margin="0,0,10,0"
                    Click="RefreshButton_Click"/>
            <CheckBox x:Name="AutoRefreshCheckBox"
                    Content="Auto Refresh (10s)"
                      VerticalAlignment="Center"
                    IsChecked="True"
                    Margin="0,0,10,0"
                      Checked="AutoRefreshCheckBox_Changed"
                    Unchecked="AutoRefreshCheckBox_Changed"/>
            <TextBlock x:Name="LastRefreshLabel"
                    VerticalAlignment="Center"
                       Foreground="Gray"
                    FontSize="12"/>
        </StackPanel>

        <!-- Data Grid -->
        <DataGrid Grid.Row="2"
                x:Name="ExchangeRateDataGrid"
                AutoGenerateColumns="False"
                  IsReadOnly="True"
                AlternatingRowBackground="#F0F0F0"
                  GridLinesVisibility="Horizontal"
                CanUserSortColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"
                        Binding="{Binding Id}"
                        Width="60"/>
                <DataGridTextColumn Header="Currency"
                        Binding="{Binding CurrencyCode}"
                        Width="100"/>
                <DataGridTextColumn Header="Rate (VND)"
                        Binding="{Binding Rate, StringFormat=N4}"
                        Width="150"/>
                <DataGridTextColumn Header="Timestamp"
                        Binding="{Binding Timestamp, StringFormat=dd/MM/yyyy HH:mm:ss}"
                        Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="3"
                Margin="0,5,0,0">
            <StatusBarItem>
                <TextBlock x:Name="RecordCountLabel"
                        Text="Records: 0"/>
            </StatusBarItem>
            <Separator/>
            <StatusBarItem>
                <TextBlock x:Name="StatusLabel"
                        Text="Ready"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
